# ADR-001 — Ligação direta por SSH

Estado: **aceite, ainda não implementado**.

O MVP ligará diretamente do cliente Windows aos servidores Ubuntu/macOS por SSH, sem backend, Prometheus ou Grafana. A implementação fica reservada para um milestone posterior dedicado. Os Milestones 1, 2 e 2.5 não contêm código SSH.
